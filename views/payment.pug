- msat2sat = require('fmtbtc').msat2sat

.modal.fade
 .modal-dialog.modal-sm
   .modal-content
     .modal-body.text-center
       h5 שלמי ברשת הברק
       p.font-weight-light.small #{ quoted_amount } ש"ח ≈ #{ msat2sat(msatoshi, true) } סטושים
       img.d-block.w-100.mb-3(src=qr)
       .input-group
         input.form-control(type='text', value=payreq, readonly)
         input-group-append: a.btn.btn-primary(href='lightning:'+payreq) ⚡
       p.text-muted.small.font-weight-light.mb-0 בקשת התשלום תקפה לעוד #[span(data-countdown-to=expires_at)]
